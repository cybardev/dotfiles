# float currently focused window
cmd + ctrl - f : yabai -m window --toggle float

# create workspace on current display
cmd + ctrl - c : yabai -m space --create

# remove current workspace and switch to the first workspace on current display
cmd + ctrl - d : yabai -m space --destroy

# move focused window to previous workspace
alt + ctrl - left : yabai -m window --space prev

# move focused window to next workspace
alt + ctrl - right : yabai -m window --space next

# cycle windows counterclockwise
cmd + ctrl - right : bash ~/.config/yabai/cycle_windows.sh

# cycle windows clockwise
cmd + ctrl - left : bash ~/.config/yabai/cycle_windows_reverse.sh

